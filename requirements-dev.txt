# Development dependencies for PitchTracker
-r requirements.txt

# Code quality
black==26.3.1
mypy==1.7.1
flake8==6.1.0

# Profiling
py-spy==0.3.14
memory-profiler==0.61.0

# Security
pip-audit==2.9.0

# Test reliability (fail fast on hangs instead of blocking CI)
pytest==9.0.3
pytest-cov==7.1.0
hypothesis==6.92.2
pytest-timeout==2.4.0

# Qt test harness (qtbot fixture used across UI tests)
pytest-qt==4.5.0

# Parallel test execution (keeps the full suite well under the CI timeout)
pytest-xdist==3.8.0
