## Supported runtimes: Python 3.13 and newer.
opencv-contrib-python==4.10.0.84
# NumPy 2.2 does not ship a CPython 3.14 Windows wheel and can crash during
# import. Keep the 3.13 pin stable while selecting the first 3.14-compatible
# wheel for newer interpreters.
numpy==2.2.2; python_version < "3.14"
numpy==2.3.5; python_version >= "3.14"
scipy==1.17.0
PyYAML==6.0.1
PySide6==6.10.1
Pillow>=10.0.0

# Pattern detection
scikit-learn==1.8.0
matplotlib==3.10.1

# Logging
loguru==0.7.2

# Validation
jsonschema==4.20.0

# System hardening - resource monitoring
psutil==7.2.1

# Secure credential storage (optional - uses OS keyring)
keyring>=24.0.0

# TAG Sports Integration (Phase 3: Bluetooth PC Ingest)
# bleak==0.21.1  # Bluetooth Low Energy library (cross-platform)
# Uncomment when Phase 3 development begins (Months 7-9)
